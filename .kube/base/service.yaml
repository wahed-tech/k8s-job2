apiVersion: v1
kind: Service
metadata:
  name: k8sjob
  namespace: wahed
  labels:
    app: k8sjob
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "2112"
    prometheus.io/path: /metrics
spec:
  ports:
    - port: 8080
      name: http
  selector:
    app: k8sjob
